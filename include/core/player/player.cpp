#include "player.hpp"

core::board::move bestmove(core::board::position position)
{
	core::board::point from;
	core::board::point to;
	core::board::move::move _move(from, to);
	
	return _move;
}